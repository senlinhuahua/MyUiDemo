<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forest.myuidemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".materialdesign.MaterialActivity" />
        <activity android:name=".qbehavior.BeHaviorActivity" />
        <activity android:name=".handrecycleview.RecycleActivity" />
        <activity android:name=".vlayout.VLayoutActivity" />
        <activity android:name=".liushiLayout.LiuShiActivity" />
        <activity android:name=".svg.SVGActivity" />
        <activity android:name=".douyinview.DouYinActivity" />
        <activity android:name=".svganime.SVGAnimeActivity" />
        <activity android:name=".qzoneview.QzoneActivity" />
        <activity android:name=".pathmeasure.PathMeasureActivity" />
    </application>

</manifest>